/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MultipPipe } from './multip.pipe';

describe('MultipPipe', () => {
  it('create an instance', () => {
    const pipe = new MultipPipe();
    expect(pipe).toBeTruthy();
  });
});
